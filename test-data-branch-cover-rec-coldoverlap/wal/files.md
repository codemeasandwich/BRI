## Directory Structure

```
wal/
├── 000000.wal
├── 000001.wal
├── 000002.wal
└── 000003.wal
```

## Files

### `000000.wal`

WAL segment fixture for integration/E2E tests.

### `000001.wal`

WAL segment fixture for integration/E2E tests.

### `000002.wal`

WAL segment fixture for integration/E2E tests.

### `000003.wal`

WAL segment fixture for integration/E2E tests.
