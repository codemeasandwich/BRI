## Directory Structure

```
wal/
├── 000000.wal
├── 000001.wal
├── 000002.wal
├── 000003.wal
├── 000004.wal
├── 000005.wal
├── 000006.wal
├── 000007.wal
├── 000008.wal
├── 000009.wal
├── 000010.wal
├── 000011.wal
├── 000012.wal
├── 000013.wal
├── 000014.wal
├── 000015.wal
├── 000016.wal
├── 000017.wal
├── 000018.wal
├── 000019.wal
├── 000020.wal
└── 000021.wal
```

## Files

### `000000.wal`

WAL segment fixture for integration/E2E tests.

### `000001.wal`

WAL segment fixture for integration/E2E tests.

### `000002.wal`

WAL segment fixture for integration/E2E tests.

### `000003.wal`

WAL segment fixture for integration/E2E tests.

### `000004.wal`

WAL segment fixture for integration/E2E tests.

### `000005.wal`

WAL segment fixture for integration/E2E tests.

### `000006.wal`

WAL segment fixture for integration/E2E tests.

### `000007.wal`

WAL segment fixture for integration/E2E tests.

### `000008.wal`

WAL segment fixture for integration/E2E tests.

### `000009.wal`

WAL segment fixture for integration/E2E tests.

### `000010.wal`

WAL segment fixture for integration/E2E tests.

### `000011.wal`

WAL segment fixture for integration/E2E tests.

### `000012.wal`

WAL segment fixture for integration/E2E tests.

### `000013.wal`

WAL segment fixture for integration/E2E tests.

### `000014.wal`

WAL segment fixture for integration/E2E tests.

### `000015.wal`

WAL segment fixture for integration/E2E tests.

### `000016.wal`

WAL segment fixture for integration/E2E tests.

### `000017.wal`

WAL segment fixture for integration/E2E tests.

### `000018.wal`

WAL segment fixture for integration/E2E tests.

### `000019.wal`

WAL segment fixture for integration/E2E tests.

### `000020.wal`

WAL segment fixture for integration/E2E tests.

### `000021.wal`

WAL segment fixture for integration/E2E tests.
