## Directory Structure

```
wal/
├── 000000.wal
├── 000001.wal
├── 000002.wal
├── 000003.wal
├── 000004.wal
├── 000005.wal
├── 000006.wal
├── 000007.wal
└── 000008.wal
```

## Files

### `000000.wal`

WAL segment fixture for integration/E2E tests.

### `000001.wal`

WAL segment fixture for integration/E2E tests.

### `000002.wal`

WAL segment fixture for integration/E2E tests.

### `000003.wal`

WAL segment fixture for integration/E2E tests.

### `000004.wal`

WAL segment fixture for integration/E2E tests.

### `000005.wal`

WAL segment fixture for integration/E2E tests.

### `000006.wal`

WAL segment fixture for integration/E2E tests.

### `000007.wal`

WAL segment fixture for integration/E2E tests.

### `000008.wal`

WAL segment fixture for integration/E2E tests.
